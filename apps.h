#ifndef _APPS_H_
#define _APPS_H_





int login(char** argv);

int getty(char** argv);

int echo(char** argv);

int wc(char** argv);

int cat(char** argv);

int sleep (char** argv);

int xargs (char** argv);

int at (char** argv);

int pr_uptime (char** argv);

int f_stat (char** argv);

int grep (char** argv);


#endif
