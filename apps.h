#ifndef _APPS_H_
#define _APPS_H_





int login(char** argv);

int getty(char** argv);

int echo(char** argv);

int cat(char** argv);
int cap(char** argv);

int sleep (char** argv);

int xargs (char** argv);

int pr_uptime (char** argv);

int f_stat (char** argv);
int f_mknod (char** argv);

int grep (char** argv);

int repeat (char** argv);

#endif
